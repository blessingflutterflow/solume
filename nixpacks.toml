[phases.setup]
cmds = ["npm install -g pnpm@9"]

[phases.install]
cmds = ["pnpm install --frozen-lockfile"]

[phases.build]
cmds = ["pnpm --filter @solune/api build"]

[start]
cmd = "node apps/api/dist/main.js"
